import AccountPage from "@/modules/account/AccountPage";
import React from "react";

const Account = () => {
    return (
        <>
            <AccountPage />
        </>
    );
};

export default Account;
